server:
  port: 9003
spring:
  application:
    name: spider-nest
zookeeper:
  address: localhost:2181
  retryTimes: 10
  retryInterval: 5000
policy:
  taskPolicy: noAction
  serverShutDownPolicy: cleanTask
#logging:
#  path: /home/ds02/bigdataplatform/logs

